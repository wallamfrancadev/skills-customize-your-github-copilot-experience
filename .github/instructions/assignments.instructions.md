---
applyTo: "assignments/**/*.md"
---

This file indicates that editor or automation instructions in this repository
apply to all markdown files under the `assignments/` directory. The `applyTo` pattern
is used by repository tooling to locate and enforce content standards for
assignment README files (for example ensuring `🎯 Objective` and `📝 Tasks`
sections exist and follow the project template).
